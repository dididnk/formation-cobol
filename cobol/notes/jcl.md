# Importants pour débloquer le JCL

  * `ipl 0a80` → démarrer la machine
  * `r 04, cancel` → répondre à une erreur
  * `s shutdown` → arrêter la session
  * `v net,act,id=LCL701`: ACTIVER L'ÉCRAN
  * `LOGON IBMUSER` → se connecter